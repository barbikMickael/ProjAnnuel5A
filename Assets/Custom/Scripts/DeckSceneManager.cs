using UnityEngine;
using System.Collections;

public class DeckSceneManager : SceneManager {

    
	// Use this for initialization
	void Start () {
        cameraArray[0].enabled = true;
        animationsArray[0].Play();
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
